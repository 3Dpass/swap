// Token symbol to icon file mapping
// Key - token symbol in UPPERCASE (e.g., "TEST", "P3D")
// Value - icon filename in src/assets/img/tokens/ directory
// Supports both .svg and .png formats
export const TOKEN_ICONS: Record<string, string> = {
	P3D: "P3D.svg",
	TEST: "TEST.png",
};

export const DEFAULT_TOKEN_ICON = "default-token.svg";
